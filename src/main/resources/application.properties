
# Configura��es do JDBC

spring.datasource.url=jdbc:mysql://localhost/teste

spring.datasource.username=root

spring.datasource.password=root

# Descomentar linha abaixo caso o banco utilizado seja PostgreSql

#spring.datasource.driver-class-name=org.postgresql.Driver

# Comentar linha abaixo caso o banco utilizado seja PostgreSql

spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Configura��es de JPA

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# Configura��es de Hibernate

spring.jpa.hibernate.ddl-auto=update

# Descomentar linha abaixo caso o banco utilizado seja PostgreSql

#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Comentar linha abaixo caso o banco utilizado seja PostgreSql

spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
